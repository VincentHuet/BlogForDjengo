<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :subject, unique: true %>
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :category %>
    <!-- <input type="text" name="category" id="category" liste="category">
    <datalist id="category">
      <% @article_array = Article.all %>   
      <% @article_array.each do |article| %>
        <option value="<%= article.category %>">
      <% end %>
    </datalist> -->
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :time %>
    <%= f.text_field :time, :value => Time.new.to_s[0..15] %>
  </div>
  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content, :rows => 3 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
