<% if @comment.new_record? %>
	$('input#comments').effect('highlight', { color: 'red' });
<% else %>
	$('div#comments').append("<%= escape_javascript(render @comment) %>");
	$('div#<%= dom_id(@comment) %>').effect('highlight');
<% end %>
